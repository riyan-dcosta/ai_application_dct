import 'package:flutter/material.dart';

class SSTArabToEngScreen extends StatelessWidget {
  const SSTArabToEngScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Arabic To English"),
        centerTitle: true,
      ),
    );
  }
}
